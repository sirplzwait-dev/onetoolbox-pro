const $=id=>document.getElementById(id),video=$("video"),file=$("file"),choose=$("choose"),start=$("start"),stop=$("stop"),result=$("result"),canvas=$("canvas"),ctx=canvas.getContext("2d");let stream=null,loop=null;
choose.onclick=()=>file.click();file.onchange=e=>{const f=e.target.files[0];if(!f)return;const u=URL.createObjectURL(f),im=new Image();im.onload=()=>{canvas.width=im.naturalWidth;canvas.height=im.naturalHeight;ctx.drawImage(im,0,0);const q=window.jsQR(ctx.getImageData(0,0,canvas.width,canvas.height).data,canvas.width,canvas.height);result.textContent=q?q.data:"No QR code found.";URL.revokeObjectURL(u)};im.src=u};
start.onclick=async()=>{try{stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:"environment"}});video.srcObject=stream;video.style.display="block";scan()}catch(e){result.textContent="Camera access was denied or is unavailable."}};
function scan(){if(!stream)return;const w=video.videoWidth||640,h=video.videoHeight||480;canvas.width=w;canvas.height=h;ctx.drawImage(video,0,0,w,h);const q=window.jsQR(ctx.getImageData(0,0,w,h).data,w,h);if(q){result.textContent=q.data;stopCam()}else loop=requestAnimationFrame(scan)}
stop.onclick=stopCam;function stopCam(){if(loop)cancelAnimationFrame(loop);stream?.getTracks().forEach(t=>t.stop());stream=null;video.style.display="none"}