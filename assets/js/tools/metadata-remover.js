const $=id=>document.getElementById(id),file=$("file"),choose=$("choose"),preview=$("preview"),status=$("status"),download=$("download");let blob=null;
choose.onclick=()=>file.click();file.onchange=async e=>{const f=e.target.files[0];if(!f)return;blob=f;const url=URL.createObjectURL(f);const im=new Image();im.onload=()=>{const c=document.createElement("canvas");c.width=im.naturalWidth;c.height=im.naturalHeight;c.getContext("2d").drawImage(im,0,0);c.toBlob(b=>{blob=b;preview.src=URL.createObjectURL(b);preview.style.display="block";download.disabled=false;status.textContent="Metadata-stripped copy is ready. Re-encoding removes metadata commonly stored outside the pixel data."},"image/jpeg",.95);};im.src=url};download.onclick=()=>{if(!blob)return;const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="clean-image - OneToolBox.jpg";a.click()}