"use strict";document.addEventListener("DOMContentLoaded",()=>{const $=id=>document.getElementById(id),fileInput=$("file"),drop=$("drop"),orig=$("orig"),ico=$("ico"),ut=$("ut"),ui=$("ui"),name=$("name"),size=$("size"),dim=$("dim"),go=$("go"),reset=$("reset"),out=$("out"),empty=$("empty"),os=$("os"),od=$("od"),download=$("download"),quality=$("quality"),qv=$("qv");let file=null,url=null,resultUrl=null,bg="white";const fmt=n=>n<1024?n+" B":n<1048576?(n/1024).toFixed(2)+" KB":(n/1048576).toFixed(2)+" MB";function load(f){if(!f)return;if(f.type!=="image/png")return alert("Please choose a PNG image.");file=f;if(url)URL.revokeObjectURL(url);url=URL.createObjectURL(f);const im=new Image();im.onload=()=>{orig.src=url;orig.style.display="block";ico.style.display=ut.style.display=ui.style.display="none";name.textContent=f.name;size.textContent=fmt(f.size);dim.textContent=`${im.naturalWidth} × ${im.naturalHeight} px`;go.disabled=false};im.src=url}fileInput.addEventListener("change",e=>load(e.target.files[0]));drop.addEventListener("dragover",e=>{e.preventDefault();drop.classList.add("dragover")});drop.addEventListener("dragleave",()=>drop.classList.remove("dragover"));drop.addEventListener("drop",e=>{e.preventDefault();drop.classList.remove("dragover");load(e.dataTransfer.files[0])});document.addEventListener("paste",e=>{for(const i of e.clipboardData?.items||[])if(i.type==="image/png"){load(i.getAsFile());break}});quality.addEventListener("input",()=>qv.textContent=quality.value+"%");document.querySelectorAll(".opts button").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".opts button").forEach(x=>x.classList.remove("active"));b.classList.add("active");bg=b.dataset.bg}));go.addEventListener("click",()=>{if(!file)return;const im=new Image();im.onload=()=>{const c=document.createElement("canvas");c.width=im.naturalWidth;c.height=im.naturalHeight;const ctx=c.getContext("2d");ctx.fillStyle=bg==="black"?"#000":"#fff";ctx.fillRect(0,0,c.width,c.height);ctx.drawImage(im,0,0);c.toBlob(blob=>{if(!blob)return;if(resultUrl)URL.revokeObjectURL(resultUrl);resultUrl=URL.createObjectURL(blob);out.src=resultUrl;out.style.display="block";empty.style.display="none";os.textContent=fmt(blob.size);od.textContent=`${c.width} × ${c.height} px`;download.href=resultUrl;download.download=file.name.replace(/\.png$/i,"")+" - OneToolBox.jpg";download.classList.remove("disabled")},"image/jpeg",Number(quality.value)/100)};im.src=url});reset.addEventListener("click",()=>{fileInput.value="";if(url)URL.revokeObjectURL(url);if(resultUrl)URL.revokeObjectURL(resultUrl);file=url=resultUrl=null;orig.removeAttribute("src");orig.style.display="none";ico.style.display=ut.style.display=ui.style.display="";name.textContent="-";size.textContent="0 KB";dim.textContent="0 × 0 px";go.disabled=true;out.removeAttribute("src");out.style.display="none";empty.style.display="block";os.textContent="0 KB";od.textContent="0 × 0 px";download.classList.add("disabled");download.removeAttribute("href")})});